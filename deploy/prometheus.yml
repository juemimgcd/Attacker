global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: attacker
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
    authorization:
      type: Bearer
      credentials_file: /run/secrets/metrics_api_key
