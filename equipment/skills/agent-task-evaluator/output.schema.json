{
  "type": "object",
  "required": ["benchmark"],
  "properties": {
    "benchmark": {
      "type": "object",
      "required": ["outcome", "reason", "evidence", "metrics"],
      "properties": {
        "outcome": {"enum": ["passed", "failed", "inconclusive", "invalid"]},
        "reason": {"type": "string", "minLength": 1},
        "evidence": {"type": "object"},
        "metrics": {"type": "object"}
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
